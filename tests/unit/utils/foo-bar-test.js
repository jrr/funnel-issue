import { module, test } from 'qunit';

module('Unit | Utility | foo-bar', function(/*hooks*/) {

  test('foobar test', function(assert) {
    assert.ok(false,"we want to exclude this test");
  });
});
